# SU

Location of `sudo` users:

```text

/etc/sudoers

```

Edit list of `sudo` users:

```text

visudo

```

Show environment variables for current user:

```text

env

```

Exit from `su`:

```text

exit

```
