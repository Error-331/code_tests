'use strict';

// external imports

// local imports

// implementation
function getRandomInt(min, max) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
}

// exports
export {
    getRandomInt,
}
